# 翻译牛 Translate Ox 隐私政策

最后更新:2026-08-27

**需要自行托管后把 URL 填进 Chrome Web Store 的隐私政策字段。**
GitHub Pages 足够:仓库开 Pages,把这个文件重命名为 `index.md` 或转成 HTML 即可。

---

## 简短版

翻译牛没有服务器。我们不收集、不存储、不传输你的任何数据。

默认情况下,翻译在你自己的电脑上完成,你的消息不会产生任何网络请求。

---

## 我们收集什么

**没有。** 具体来说:

- 我们不运营任何后端服务器,技术上没有接收你数据的地方
- 没有分析、遥测、崩溃上报或使用统计
- 不收集你的身份、邮箱、IP 地址或设备信息
- 不使用 Cookie 或任何跟踪技术

## 你的消息如何被处理

### 默认引擎(Chrome 内置翻译)

翻译由 Chrome 浏览器内置的设备端翻译模型完成,**完全在你的电脑本地运行**,
不产生网络请求。首次使用某个语言对时,Chrome 会从 Google 下载对应的离线语言包 ——
这是浏览器自身的行为,下载的是模型文件,不包含你的任何消息内容。

### 进阶引擎(可选,需要你自己的 API 密钥)

如果你在设置中填入自己的 DeepL、OpenAI 或 DeepSeek 密钥并启用该引擎,
你要翻译的消息内容会**用你自己的密钥、从你的浏览器直接发送给该服务商**。

### 语法检查(可选,必须有你自己的 API 密钥)

按快捷键检查语法时,输入框里那段文字会**用你自己的密钥、从你的浏览器直接发送给
你配置的那家 LLM 服务商**,和上面的进阶翻译走同一条路径、同一个密钥。

这个功能**没有本地版本**:Chrome 内置的翻译引擎只能翻译,做不了语法校对。
所以没填密钥时它会明确拒绝并提示,而不是悄悄降级成别的东西。

它**只在你主动按下快捷键的那一刻发送一次**,发送的只有输入框里的当前内容 ——
不发送对话上下文,不发送其他消息,也不会在后台持续检查你打的字。

这些数据不经过我们的任何设施 —— 我们没有设施。你与该服务商之间的数据处理
适用他们各自的隐私政策:

- DeepL: https://www.deepl.com/privacy
- OpenAI: https://openai.com/policies/privacy-policy
- DeepSeek: https://www.deepseek.com/privacy

启用进阶引擎时,浏览器会单独向你申请访问对应域名的授权。你不启用就永远不会发生。

如果你在设置中把"上下文条数"设为大于 0 的值,该对话中最近的若干条消息也会
一并发送给上述服务商,用于帮助模型理解指代关系。**把它设为 0 可以完全禁止这一行为。**

### 在 WhatsApp 以外的网站上(通用模式)

你可以在任意网站的输入框里按快捷键翻译。这个功能的工作方式是:

- 扩展**平时不在这些网站上运行**,也没有常驻的访问权限。你按下快捷键的那一刻,
  浏览器临时授予当前标签页的访问权(Chrome 的 `activeTab` 机制),扩展才注入翻译逻辑,
  用完即失效。你可以在扩展详情页核对:权限列表里只有 web.whatsapp.com,没有"所有网站"。
- 被读取的只有**当前聚焦的那一个输入框里的文字**。扩展不读取页面上的其他内容,
  不读取网址,不读取页面标题,也不遍历页面上的其他表单。
- 这些文字的处理方式与上文完全相同:默认引擎在本地翻译、不联网;
  只有你自己启用了进阶引擎,它才会用你的密钥发给对应服务商。
- 原文只保存在内存里(供你按第二次快捷键切回),不写入磁盘,页面刷新即消失。
- 不喜欢这个功能可以在设置里整体关掉("其他网站"那一栏)。

## 数据存在哪里

存储在你浏览器的本地存储(`chrome.storage.local`)中,仅限本机,不参与 Chrome 账号同步:

- 你的界面偏好(默认引擎、目标语言、开关状态)
- 你填入的 API 密钥
- 页面结构适配规则的缓存(纯技术配置,与你的数据无关)

**消息内容从不写入磁盘。** 已翻译的消息只保存在内存中(用于避免滚动时重复翻译),
关闭标签页即全部消失。

卸载扩展会清除以上全部内容。

## 我们对你的账号做什么

不做任何事。本扩展不会替你发送消息、不会自动回复、不会批量操作、不读取你的联系人列表、
不上传你的聊天记录。它只在你主动按下快捷键或点击按钮时,替换输入框中的文字。

## 权限说明

- **web.whatsapp.com 的访问权限**:读取输入框内容以翻译、将译文写回输入框、
  在收到的消息下方附加译文。这是扩展工作所必需的最小权限,且仅限此站点。
- **存储权限**:保存上述设置。
- **闹钟权限**:定期检查页面结构适配规则的更新。
- **activeTab + scripting(通用模式)**:仅在你主动按下快捷键的那一刻,
  临时获得当前标签页的访问权并注入翻译逻辑,用完即失效。**它不是常驻权限,
  也不等于"访问所有网站"** —— 正因为用了这个机制,你在安装时才不会看到
  "读取和更改你在所有网站上的数据"这类提示。详见上文「通用模式」一节。
- **第三方 API 域名**:仅在你启用进阶引擎时运行时申请,默认安装不申请。

## 儿童隐私

本扩展不面向 13 岁以下儿童,也不会有意收集其信息(实际上我们不收集任何人的信息)。

## 政策变更

如果未来版本引入任何数据收集行为,我们会在本页更新并在扩展更新说明中明确告知。
承诺不做静默变更。

## 联系方式

<!-- 填入你的邮箱或 GitHub Issues 地址 -->
